# Mock 环境部署说明

- 前置：Docker 与 Compose 可用。
- 启动：`make up`；测试与覆盖率：`make coverage`；报告：`make report && make coverage-report`；下线：`make down`。
- 说明：Mock 接口位于 `tests/mocks/*`；前端占位页在 `docs/ui/oauth-guide.html`。
